# Copy to .env and fill in. .env is gitignored and must never be committed.
# Reused from the isramarket project's working configuration.
GEMINI_API_KEY=
GEMINI_IMAGE_MODEL=gemini-3-pro-image
GEMINI_IMAGE_SIZE=2KGEMINI_TEXT_MODEL=gemini-3-flash-preview
